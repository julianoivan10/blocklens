import { DashboardOverview } from '@/features/dashboard/dashboard-overview';

export const metadata = {
  title: 'Dashboard | BlockLens',
};

export default function DashboardPage() {
  return <DashboardOverview />;
}
